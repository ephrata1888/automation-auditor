# automation-auditor

A multi-agent forensic evaluation system built with LangGraph.

## Setup

1. `uv install`
2. Copy `.env.example` to `.env` and add your API keys

## Run

```bash
python -m src.graph